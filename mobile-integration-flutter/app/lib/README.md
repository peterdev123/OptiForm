# Flutter lib scaffold (Phase 1.5)

This scaffold is pre-created before `flutter create .` is available.

After Flutter is installed and `flutter create .` is executed, keep these folders and integrate them into the generated app structure.
